/* 
Git log para saber que tenemos cargado y en que rama
PAra ver las ramas = git branch
Para crear una nueva rama = git checkout -b "nombre de la rama". (ej: git checkout -b poo )
para cambiar de rama = git checkout "nombre de la rama" (ej: git checkout main )
para mezclar las rama, 1. ubicarse en la carpeta principal, 2. git merge "nombre de la rama" (git merge poo)
*/